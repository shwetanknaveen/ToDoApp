<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:todo3="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/navAddTask"
            android:icon="@drawable/ic_add_task"
            android:title="@string/add_task" />
        <item
            android:id="@+id/navViewByDate"
            android:icon="@drawable/ic_view_by_date"
            android:title="@string/view_by_date" />

        <item
            android:id="@+id/navShowUncompTasks"
            android:icon="@drawable/ic_uncom_task"
            android:title="@string/show_uncompleted_tasks" />
        <item
            android:id="@+id/navManageTags"
            android:icon="@drawable/ic_manage_tags"
            android:title="@string/manage_tags" />
    </group>

    <item android:title="Modify view">
        <menu>

            <item
                android:id="@+id/navSortByDate"
                android:icon="@drawable/sort_by_date_icon"
                android:title="Sort by Date"
                todo3:actionLayout="@layout/layout_switch_date"
                />
            <item
                android:id="@+id/navSortByTag"
                android:icon="@drawable/sort_by_tag_icon"
                android:title="Sort by tags"
                todo3:actionLayout="@layout/layout_switch_tag"
                />
        </menu>
    </item>




</menu>
